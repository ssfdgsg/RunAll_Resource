# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ResourceService API
    version: 0.0.1
paths:
    /v1/instances/{instanceId}/ports:
        post:
            tags:
                - ResourceService
            description: 2. 设置实例端口暴露
            operationId: ResourceService_SetInstancePort
            parameters:
                - name: instanceId
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/resource.v1.SetInstancePortReq'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/resource.v1.SetInstancePortResp'
    /v1/resources:
        get:
            tags:
                - ResourceService
            description: 1. 列出全部资源
            operationId: ResourceService_ListResources
            parameters:
                - name: userId
                  in: query
                  schema:
                    type: string
                - name: start
                  in: query
                  schema:
                    type: string
                    format: date-time
                - name: end
                  in: query
                  schema:
                    type: string
                    format: date-time
                - name: type
                  in: query
                  schema:
                    type: string
                - name: fieldMask
                  in: query
                  schema:
                    type: string
                    format: field-mask
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/resource.v1.ListResourcesReply'
components:
    schemas:
        resource.v1.ListResourcesReply:
            type: object
            properties:
                resources:
                    type: array
                    items:
                        $ref: '#/components/schemas/resource.v1.Resource'
                specs:
                    type: object
                    additionalProperties:
                        $ref: '#/components/schemas/resource.v1.ResourceSpec'
        resource.v1.PortConfig:
            type: object
            properties:
                port:
                    type: integer
                    format: uint32
                protocol:
                    type: string
                ingressDomain:
                    type: string
        resource.v1.PortResult:
            type: object
            properties:
                port:
                    type: integer
                    format: uint32
                success:
                    type: boolean
                accessUrl:
                    type: string
                error:
                    type: string
        resource.v1.Resource:
            type: object
            properties:
                instanceId:
                    type: string
                name:
                    type: string
                userId:
                    type: string
                type:
                    type: string
                createdAt:
                    type: string
                    format: date-time
                updatedAt:
                    type: string
                    format: date-time
            description: =====================实体/值对象=======================
        resource.v1.ResourceSpec:
            type: object
            properties:
                instanceId:
                    type: string
                cpuCores:
                    type: integer
                    format: uint32
                memorySize:
                    type: integer
                    format: uint32
                gpu:
                    type: integer
                    format: uint32
                image:
                    type: string
                customConfig:
                    type: object
        resource.v1.SetInstancePortReq:
            type: object
            properties:
                instanceId:
                    type: string
                open:
                    type: boolean
                portConfigs:
                    type: array
                    items:
                        $ref: '#/components/schemas/resource.v1.PortConfig'
            description: 2. 设置实例端口暴露
        resource.v1.SetInstancePortResp:
            type: object
            properties:
                success:
                    type: boolean
                results:
                    type: array
                    items:
                        $ref: '#/components/schemas/resource.v1.PortResult'
                message:
                    type: string
tags:
    - name: ResourceService
